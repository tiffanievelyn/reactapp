import React, { Component } from 'react';

import strings from './_resources/Strings';

class Insights extends Component {
    render(){
        return (
            <div>
                <h1>{strings.label_insights}</h1>
            </div>
        )
    }
}

export default Insights;